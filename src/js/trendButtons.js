function pleaseShow() {
  const element = document.getElementById('trendBox');
  element.classList.toggle('show');
}
function current() {
  const currentBtn = document.getElementsByClassName('assortment__btn');
  console.log(currentBtn);
  currentBtn.addEventListener.classList.add('current');
}
